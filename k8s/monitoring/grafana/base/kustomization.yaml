apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  # - manifests.yaml  # Generated via: helm template (see k8s/monitoring/README.md)
  []

# Note: Run the Helm template command from README.md to generate manifests.yaml
