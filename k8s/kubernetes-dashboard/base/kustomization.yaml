apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubernetes-dashboard

resources:
  # namespace is included in install.yaml
  - install.yaml
  - admin-user.yaml
  - certificate.yaml
  - servers-transport.yaml
  - ingress-route.yaml
